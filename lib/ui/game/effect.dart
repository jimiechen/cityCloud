import 'dart:ui';

import 'package:flame/components/component.dart';

class CustomEffectComponent extends SpriteComponent {
  bool haveAddEffect = false;
  @override
  void render(Canvas c) {
    super.render(c);
  }
}
